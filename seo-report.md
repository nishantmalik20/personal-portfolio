# SEO / GEO Report — inishant.com portfolio

_Generated by Agent 06 (SEO/GEO). Last updated: 2026-06-15._

This is Nishant's **personal portfolio** (single-page, one route `/`), so the
client-business parts of the agent spec were adapted: `Person` + `ProfessionalService`
schema instead of full-NAP `LocalBusiness`, and **no FAQ section was injected** into
the hand-tuned 3D storybook (see Recommendations).

## What was added

| Area | File | Notes |
|------|------|-------|
| Root metadata | `app/layout.tsx` | canonical, keywords, authors/creator/publisher, `robots` (+ googleBot max-image-preview), OpenGraph (url/siteName/`en_CA`), Twitter `summary_large_image`. `lang` set to `en-CA`. |
| Structured data | `components/seo/json-ld.tsx` | `@graph` injected site-wide in the layout. |
| Sitemap | `app/sitemap.ts` | → `/sitemap.xml` |
| Robots | `app/robots.ts` | → `/robots.txt`, allows all (incl. AI bots), blocks `/api/` |
| Social image | `app/opengraph-image.tsx` + `app/twitter-image.tsx` | Dynamic 1200×630 branded card via `next/og` (storybook palette). |

## Structured data (JSON-LD @graph)

- **Person** (`#person`) — Nishant Malik, jobTitle, email, `alumniOf` (MDU Rohtak,
  Humber, U Manitoba), `knowsAbout` (full stack + web design/SEO/AI), `sameAs`
  (GitHub, LinkedIn, Instagram), Winnipeg `PostalAddress`, `worksFor` the business.
- **ProfessionalService** (`#business`) — the freelance practice (inishant.com),
  `areaServed` Winnipeg → Manitoba → Canada, `makesOffer` (Design / Develop / Care),
  founder = the Person, `priceRange` `$$`.
- **WebSite** (`#website`) — the site entity, `publisher` = the Person, `en-CA`.

All entities are cross-linked by `@id` so Google and generative engines resolve a
single, consistent knowledge graph.

## GEO (Generative Engine Optimization) status

- ✅ **All content is in the server HTML** — education, services, AI focus,
  experience, projects, contact are all server-rendered (verified against the live
  site). AI answer engines can read everything despite the 3D canvas.
- ✅ **Clean heading hierarchy** — a single `<h1>` ("Nishant") with logical H2/H3s.
- ✅ **Entity/author signals** via the Person + ProfessionalService graph.
- ✅ **Specific, factual copy** (real schools, years, stack, Winnipeg).

## Sitemap

```
https://inishant.com   (priority 1, monthly)
```
Single public route. Re-generates `lastModified` on each build.

## Pages / items needing attention

- **OG image** — ✅ now generated dynamically (was missing).
- **`apple-icon`** — not present. `app/icon.svg` covers favicons; a 180×180
  `app/apple-icon.png` would polish iOS home-screen bookmarks. _Low priority._
- **FAQ section** — strong GEO win, but adding Q&A + `FAQPage` schema means new
  content/design in the storybook scroll. _Deferred — needs your design sign-off._
- **Per-page metadata** — N/A while the site is a single page. Revisit if routes
  (e.g. `/work`, `/blog`) are added later.

## Local SEO — off-site checklist (manual, by you)

- [ ] **Google Search Console** — verify `inishant.com`, submit `/sitemap.xml`
- [ ] **Bing Webmaster Tools** — verify + submit sitemap
- [ ] **Google Business Profile** — optional for a freelancer; if created, keep
      Name/area/website consistent (service-area business, no public address needed)
- [ ] Consistent name + `hello@inishant.com` across site, profiles, directories
- [ ] (Do the GoDaddy DNS step first so `inishant.com` resolves — canonical/OG/
      sitemap all point at it)

> **Blocker:** canonical, sitemap, and OG URLs all reference `https://inishant.com`.
> They're correct, but only resolve once the GoDaddy DNS is connected. Search Console
> verification + sitemap submission should wait until the domain is live.
